# A simple todo app containing
    
    -react hooks
    -context
    -reducer

# Remember to install uuid after cloning the repo.

    -clone from github
    -type npm install
    -type npm install uuid

